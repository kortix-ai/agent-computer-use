---
"agent-cu": patch
---

agent-cu setup — interactive (and scriptable) wizard to configure Claude Code so agent-cu runs without per-command approval prompts. Supports --mode/--scope flags for non-interactive use.
